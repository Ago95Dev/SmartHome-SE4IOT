apiVersion: 1

providers:
  - name: 'Default'
    orgId: 1
    folder: ''            # Le dashboard verranno caricate nella root
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards
    allowUiUpdates: true
    updateIntervalSeconds: 10
